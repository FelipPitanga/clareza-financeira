import {sqliteTable,text,integer,primaryKey,foreignKey} from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('finance_users',{id:text().primaryKey(),revision:integer().notNull().default(0),categories:text().notNull().default('[]')});
export const accounts=sqliteTable('accounts',{owner:text().notNull().references(()=>users.id),id:text().notNull(),name:text().notNull(),scope:text().notNull(),initial:integer().notNull(),payload:text().notNull()},t=>[primaryKey({columns:[t.owner,t.id]})]);
export const cards=sqliteTable('cards',{owner:text().notNull(),id:text().notNull(),account:text().notNull(),payload:text().notNull()},t=>[primaryKey({columns:[t.owner,t.id]}),foreignKey({columns:[t.owner,t.account],foreignColumns:[accounts.owner,accounts.id]})]);
export const entries=sqliteTable('entries',{owner:text().notNull(),id:text().notNull(),account:text().notNull(),amount:integer().notNull(),date:text().notNull(),kind:text().notNull(),status:text().notNull(),payload:text().notNull()},t=>[primaryKey({columns:[t.owner,t.id]}),foreignKey({columns:[t.owner,t.account],foreignColumns:[accounts.owner,accounts.id]})]);
export const reserves=sqliteTable('reserves',{owner:text().notNull(),id:text().notNull(),account:text().notNull(),amount:integer().notNull(),payload:text().notNull()},t=>[primaryKey({columns:[t.owner,t.id]}),foreignKey({columns:[t.owner,t.account],foreignColumns:[accounts.owner,accounts.id]})]);
